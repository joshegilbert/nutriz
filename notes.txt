Backend setup:
  cd nutriz-backend
  npm install
  # Create .env with at least MONGO_URI and JWT_SECRET
  npm run start

Seed demo content:
  cd nutriz-backend
  npm run seed:sample
  # Logs credentials (coach@nutriz.demo / DemoPass123!) unless overridden

Frontend setup:
  npm install
  npm run dev -- --host

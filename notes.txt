Backend setup:
  cd nutriz-backend
  npm install
  # Create .env with at least MONGO_URI and JWT_SECRET
  npm run start

starting a server for frontend: 
    npm run dev

For github actions:

git add .

git commit -m "commit message"

git push
